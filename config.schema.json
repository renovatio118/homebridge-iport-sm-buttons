{
  "pluginAlias": "IPortSMButtons",
  "pluginType": "platform",
  "singular": true,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Name",
        "type": "string",
        "default": "iPort Buttons"
      },
      "ip": {
        "title": "Device IP",
        "type": "string"
      },
      "port": {
        "title": "Device Port",
        "type": "integer",
        "default": 10001
      },
      "timeout": {
        "title": "Connection Timeout (ms)",
        "type": "integer",
        "default": 5000
      },
      "reconnectDelay": {
        "title": "Reconnect Delay (ms)",
        "type": "integer",
        "default": 5000
      },
      "triggerResetDelay": {
        "title": "Virtual Switch Reset Delay (ms)",
        "type": "integer",
        "default": 500
      },
      "buttonMappings": {
        "title": "Button Mappings",
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "buttonNumber": {
              "title": "Button Number",
              "type": "integer",
              "minimum": 1,
              "maximum": 10
            },
            "modeColor": {
              "title": "Mode Color",
              "type": "string",
              "enum": ["any", "yellow", "red", "blue", "green", "purple", "white"]
            },
            "actionType": {
              "title": "Action Type",
              "type": "string",
              "enum": ["accessory", "url"],
              "default": "accessory"
            },
            "targetName": {
              "title": "Target Accessory Name (if Action Type = accessory)",
              "type": "string"
            },
            "action": {
              "title": "Accessory Action",
              "type": "string",
              "enum": ["on", "off", "toggle"],
              "default": "toggle"
            },
            "url": {
              "title": "URL to Trigger (if Action Type = url)",
              "type": "string"
            }
          }
        }
      }
    }
  }
}
